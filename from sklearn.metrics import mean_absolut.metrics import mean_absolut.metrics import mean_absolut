from sklearn.metrics import mean_absolute_error, mean_squared_error
import math

# Calcular el MAE y RMSE
mae = mean_absolute_error(y_test_descaled, predictions)
rmse = math.sqrt(mean_squared_error(y_test_descaled, predictions))

print(f"Error Medio Absoluto (MAE): {mae}")
print(f"Raíz del Error Cuadrático Medio (RMSE): {rmse}")

# Obtener las fechas correspondientes
test_dates = data.index[train_size + sequence_length:]

# Visualizar los resultados
plt.figure(figsize=(14, 7))
plt.plot(test_dates, y_test_descaled, label='Precio Real')
plt.plot(test_dates, predictions, label='Predicción')
plt.title(f'Predicción de Precios para {ticker} con LSTM')
plt.xlabel('Fecha')
plt.ylabel('Precio (USD)')
plt.legend()
plt.show()
